services:
  service_test:
    build:
      context: ./service/
      dockerfile: Dockerfile.test
    tty: true
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./service/app:/app
      - ./service/data:/data
      - ./service/reports:/reports
